/**
 * Based on DnC
 * Find mid and break 2 part
 * Recursively sort
 * Merge the array
 */

#include <iostream>
using namespace std;

int main()
{
     int arr[] = {
         4, 5, 13, 2, 12};

     int size = sizeof(arr) / sizeof(int);
}